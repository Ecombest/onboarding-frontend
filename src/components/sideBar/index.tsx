import AppContext, { AppContextProps } from "@/context/app.context";
import App from "next/app";
import { useContext } from "react";

const SideBar: React.FC = () => {
  return <div className="sidebar">asdada</div>;
};
export default SideBar;
